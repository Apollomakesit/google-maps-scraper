[phases.setup]
nixPkgs = ['nodejs-22_x']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'node .next/standalone/server.js'

[variables]
NODE_ENV = 'production'
PORT = '3000'
HOSTNAME = '0.0.0.0'
