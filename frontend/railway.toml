[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"
# Note: This path is relative to the root directory setting (frontend/)

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
